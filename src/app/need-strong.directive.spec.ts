import { NeedStrongDirective } from './need-strong.directive';

describe('NeedStrongDirective', () => {
  it('should create an instance', () => {
    const directive = new NeedStrongDirective();
    expect(directive).toBeTruthy();
  });
});
